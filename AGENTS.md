# AGENTS.md

Repository agent instructions:

1. Always read `CLAUDE.md` before making changes in this repository.
2. Treat `CLAUDE.md` as the main source of truth for repository structure, workflows, and conventions.
3. When interacting with files that fall under rule-scoped guidance, also read every relevant file in `.claude/rules/*.md` before making changes.
4. Apply `.claude/rules/*.md` files based on the paths, scopes, or applicability metadata they define for the files you are touching.
5. If there is any conflict or ambiguity, follow `CLAUDE.md` first, then apply the relevant `.claude/rules/*.md` guidance for the files you are touching.
